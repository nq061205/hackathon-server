# Backend cho dev server proxy (/api -> target). Doi neu backend chay o may khac.
VITE_API_TARGET=http://localhost:8080

# Dia chi API dien san o man hinh dang nhap (doi thanh IP may chu backend neu khac may).
VITE_DEFAULT_API=http://localhost:8080
